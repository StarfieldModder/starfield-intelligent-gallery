[pytest]
minversion = 7.0
addopts = -q
markers =
    flaky: mark test as flaky; use pytest-rerunfailures to rerun
    async: mark async tests
# Optional: configure reruns globally (requires pytest-rerunfailures)
# To enable reruns via CLI: pytest --reruns 3 --reruns-delay 0.2
