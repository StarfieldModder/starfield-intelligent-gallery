# AGENTS.md — AI Coding Agent Instructions

Purpose
-------
This file gives concise, actionable guidance for AI coding agents working on Starfield Intelligent Gallery (SIG). It points to authoritative docs, runtime/test commands, and project-specific conventions so an agent can be productive immediately.

Quick commands
--------------
- **Run app (development)**: `python main.py`
- **Install editable package (optional)**: `python -m pip install -e .`
- **Invoke CLI entrypoint** (after install): `sig`
- **Run tests**: `pytest`

Key facts
---------
- **Python version**: `>=3.10` (see [pyproject.toml](pyproject.toml#L1)).
- **Project entrypoint**: `main.py` (see [main.py](main.py#L1)).
- **Package script**: `sig` → `GalleryApp.cli:main` (see [pyproject.toml](pyproject.toml#L1)).
- **Tests**: configured via [pytest.ini](pytest.ini#L1) (uses `pythonpath = .`).
- **Primary docs**: [README.md](README.md#L1) — consult for architecture and workflows.

Agent guidelines
----------------
- Run the test suite (`pytest`) before proposing or applying broad changes.
- Prefer small, focused edits with passing tests; annotate changes in PR descriptions.
- Link rather than copy large documentation: reference README and other docs.
- Preserve user-facing strings and data formats unless the change includes migration steps.
- When adding new dependencies, update `pyproject.toml` and explain rationale.

When in doubt
-----------
If behaviour or intent is unclear, ask before making opinionated architectural changes.

Next suggested customizations
---------------------------
- Add a small `agents/` skill for automated test/format checks.
- Create a `.github/copilot-instructions.md` only if project-level guidance diverges significantly from this file.

---
Generated by an AI assistant to help future automated contributors start safely.
